#include<stdio.h>
int main() {
	int a, b, c, d;
	while (scanf("%d%d%d%d", &a, &b, &c, &d) !=EOF) {
		double k;
		k = (a * a - b) / 2;//xy=k
		if ((a * b == c + k * a) && (a * c == d + k * b))
		{
			printf("Y\n");
		}
		else
			printf("N\n");
	}
	return 0;
}
