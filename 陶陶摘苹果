#include<stdio.h>
#include<string.h>
int main() {
	int i;
	int a[10];
	for (i = 0;i < 10;i++) {
		scanf("%d", &a[i]);
	}
	int h = 0;
	scanf("%d", &h);
	int sum = 0;
	for (i = 0;i < 10;i++) {
		if (a[i] <= (30 + h)) {
			sum = sum + 1;
		}
	}
	printf("%d", sum);
	return 0;
}
