#include<stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int a[100];
    int i;
    for (i = 0;i < n * n;i++) {
        scanf("%d", &a[i]);
    }
    int j, k;
    for (i = n-1;i >=0 ; i--) {
        for (j = 0;j < n;j++) {
            printf("%d ", a[i*n+j]);
        }
        printf("\n");
    }
    return 0;
}
